# Переменная для хранения произведения
multipl = 1

# Цикл по числам от 1 до 10 включительно
for i in range(1, 11):
    if i % 2 == 0:
        continue  # Пропускаем чётные числа
    multipl *= i  # Умножаем на нечётное число

# Вывод результата
print(multipl)
